"""Package-internal bundled resources (default config etc.).

Files under ``_bundled/`` are shipped inside the installed wheel so the app
has a guaranteed default even when the deploy-time filesystem layout is
different from the developer machine (e.g. DigitalOcean App Platform).
"""
